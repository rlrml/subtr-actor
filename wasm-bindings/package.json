{
  "name": "subtr-actor-wasm",
  "version": "0.1.0",
  "description": "WASM bindings for subtr-actor - Rocket League replay processing",
  "main": "pkg/subtr_actor_wasm.js",
  "types": "pkg/subtr_actor_wasm.d.ts",
  "files": [
    "pkg/*"
  ],
  "scripts": {
    "build": "wasm-pack build --target web --out-dir pkg",
    "build:nodejs": "wasm-pack build --target nodejs --out-dir pkg-node",
    "build:bundler": "wasm-pack build --target bundler --out-dir pkg-bundler",
    "test": "wasm-pack test --headless --firefox",
    "pack": "wasm-pack pack",
    "publish": "wasm-pack publish"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/rlrml/subtr-actor.git"
  },
  "keywords": [
    "rocket-league",
    "replay",
    "wasm",
    "webassembly",
    "parsing"
  ],
  "author": "Ivan Malison <ivanmalison@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "wasm-pack": "^0.12.1"
  }
}