{
  "name": "subtr-actor-player",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "dist/subtr-actor-player.js",
  "scripts": {
    "build:bindings": "env PATH=$HOME/.cargo/bin:/run/current-system/sw/bin:/usr/bin:/bin npm --prefix .. run build",
    "check": "tsc --noEmit",
    "build": "npm run build:bindings && npm run check && vite build"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/three": "^0.180.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.5"
  }
}
